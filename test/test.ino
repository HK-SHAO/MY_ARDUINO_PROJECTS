#include <IRremote.h>
#include <U8glib.h>

int tonePin = 13; //蜂鸣器的pin
int inPin = A0;

void setup() {
  //pinMode(tonePin, OUTPUT); //设置蜂鸣器的pin为输出模式
  //pinMode(inPin, INPUT);
}

void loop() {
  //tone(tonePin, map(digitalRead(inPin), 100, 2800, 0, 1000));
}
